import { Chain } from '@civic/common-gateway-react';
import { Connection, PublicKey, Transaction } from '@solana/web3.js';
import { SolanaGatewayProps } from './types';
export declare const chainImplementation: ({ connection, cluster, publicKey, signTransaction, handleTransaction, gatekeeperNetworkAddress, stage, }: {
    connection: Connection;
    cluster: string;
    publicKey: PublicKey;
    signTransaction: (transaction: Transaction) => Promise<Transaction>;
    handleTransaction?: ((transaction: Transaction) => Promise<void>) | undefined;
    gatekeeperNetworkAddress: PublicKey;
    stage: string;
}) => Chain;
export declare const useSolanaGateway: () => SolanaGatewayProps;
